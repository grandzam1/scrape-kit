---
title: "ChatGPT swimlane conversation"
source: https://chatgpt.com/share/6a886ffe-80e4-83ea-8318-3ee608609cdb
---

### Prompt
/eli10
/human

About the Swimlane Diagram Template

When processes start to get messy, it’s a good idea to take a step back and visualize who does what and when. Enter: the swimlane diagram template. This digestible, one-stop visual representation uses the metaphor of lanes in a pool to add clarity to complex processes.

A swimlane diagram takes a familiar physical place (a lap pool) and turns it into an analogy for organizing processes within a team, workgroup, department, or multilayered organization.

The template is typically divided into several lanes. Each lane is associated with a department, like development, marketing, or sales. Lanes can also correspond to entities outside your company, like customers or third-party vendors. Shapes in the lanes represent all the processes within the scope of the diagram. Arrows show how those objects feed information and/or materials to each other.

In the end, a swimlane diagram template looks like a flowchart but also incorporates elements of an org chart and value stream map (VSM). It shows you what gets done, who does it, and how everything happens.

How to use the swimlane diagram template

Get started by selecting the Miro swimlane diagram template, then follow these steps to make it your own. You can also use Miro's flowchart maker and shapes.

1. Be clear about your goals

Before you start diagramming, determine your desired outcome. Remember that swimlane flowchart templates are best for visualizing how multiple contributors or teams work through the stages of a process.

2. Name your swimlanes and start mapping out a process

Customize the swimlane flowchart by double-clicking shapes. Add the name of each team member, function, or stakeholder. Don’t forget to include outside stakeholders, like customers or contractors.

3. Add arrows to finish your flow mapping

Arrows show the flow of responsibilities from one step to the next. They can stay within lanes or cross lane boundaries. Add an arrow whenever one step triggers another or makes it possible.

Remember that there’s no wrong way to draw your pool. You can use as many lanes as your process demands and arrange them from north to south or east to west.

Why should you use a swimlane template?

Enhanced visibility and clarity: The swimlane diagram template offers a clear and organized visual representation of complex processes. With distinct swimlanes for each participant or department, team members can easily understand the flow of activities. This fosters transparency and reduces the likelihood of misunderstandings.
Efficient workflow management: The template makes streamlining and optimizing processes easier. Assign tasks to specific lanes, identify bottlenecks, and ensure each team member understands their role in the workflow. This boosts efficiency and empowers your team to work cohesively towards common objectives. If you require additional assistance with your swimlane diagram, you may find it helpful to utilize the cross-functional flowchart template.
Collaborative problem-solving: By visually representing tasks, responsibilities, and handovers, teams can spot areas that require attention or improvement. This fosters a culture of innovation and continuous improvement.
Time and resource optimization: Time is a precious commodity in any business. The swimlane template helps you identify areas where time and resources can be optimized. Visualizing the entire process allows you to identify redundant steps, streamline workflows, and allocate resources more efficiently. This improves productivity and allows your team to focus on achieving their goals.
Swimlane Diagram Template FAQs

How do you make a Swimlane Diagram?

You can start making your swimlane diagram using Miro's pre-made template. The template helps you to diagram the swimlanes easier, as you can add more rows and columns according to your needs. After you set your swimlane flowchart template, start adding the roles and process steps to it. Then, assign responsible departments or people to the swimlanes and connect the steps with arrows and lines.

Can I collaborate with my team in real time using the swimlane diagram template?

Yes, absolutely! Collaboration is at the core of Miro's design. With the Swimlane Diagram Template, team members can work together in real time, no matter where they are located. Multiple users can edit and contribute simultaneously, creating a dynamic and interactive environment for effective teamwork.

Are any customization options available for the swimlane diagram template?

Yes, the Miro swimlane diagram template is highly customizable. You can personalize the swimlanes, shapes, and text to fit your project or workflow. Choose from various colors, styles, and formatting options to make your diagram visually engaging and tailored to your team's preferences.

Can I export or share the swimlane diagram created in Miro with external collaborators?

Yes, you can! Miro offers seamless sharing and exporting options. Once you've created your swimlane diagram, you can share it with external collaborators by generating a shareable link or inviting them directly to collaborate within Miro. Additionally, you can export your diagram in various formats, such as PDF, PNG, or JPEG, making it easy to integrate into presentations or share with stakeholders outside of the Miro platform.

### Reply
## 🧠 Swimlane Diagram — ELI10

Imagine you’re watching a **relay race**.

There are different runners, and each runner has their own lane. You can easily see:

- **Who** is doing something

- **What** they are doing

- **When** they do it

- **Who receives it next**

- **Where decisions happen**

- **Where the process can get stuck**

That is basically what a **swimlane diagram** does.

### 🏊 Simple example

Imagine a customer buying something online:

| Customer | Website | Payment | Warehouse |
| --- | --- | --- | --- |
| Start |  |  |  |
| Place order → | Receive order |  |  |
|  | → Send payment request |  |  |
|  |  | Process payment |  |
|  | ← Payment approved |  |  |
|  | → Send order |  | Receive order |
|  |  |  | Ship product → |
| Receive product |  |  |  |

The **lanes represent people, teams, systems, or organizations**.

The **boxes represent actions**.

The **diamonds represent decisions**.

The **arrows represent what happens next**.

* * *

## 🎯 The easiest way to understand it

A normal flowchart tells you:

> **"What happens?"**

A swimlane diagram tells you:

> **"What happens + who does it?"**

That's the main difference.

### Normal flowchart

```

Start
  ↓
Place Order
  ↓
Process Payment
  ↓
Ship Order
  ↓
End
```

### Swimlane

```

CUSTOMER
   │
   ├── Place Order
   │        ↓
WEBSITE
   │
   ├── Receive Order
   │        ↓
PAYMENT
   │
   ├── Process Payment
   │        ↓
WAREHOUSE
   │
   └── Ship Order
```

Now you can immediately see **responsibility**.

* * *

# 🧩 What the original text is really saying

### 1\. "Processes get messy"

When many people, departments, or systems are involved, a normal flowchart can become confusing.

Swimlanes separate them.

* * *

### 2\. "Each lane represents a department"

A lane could be:

- 👤 Customer

- 💻 Website

- 💳 Payment System

- 📦 Warehouse

- 👨‍💼 Admin

- 🏢 Third-party company

It doesn't have to be a department.

**Any actor responsible for something can have a lane.**

* * *

### 3\. "Shapes represent processes"

Inside each lane, you put the things that actor does.

For example:

```

Customer
   │
   └── Enter email
        ↓
Website
   │
   └── Validate email
        ↓
Website
   │
   └── Is email valid?
        ↓
      YES / NO
```

* * *

### 4\. "Arrows show handoffs"

This is VERY important.

If the **Customer** does something and the **Website** must respond, the arrow crosses lanes.

```

Customer                 Website

[Enter email] ─────────→ [Validate email]
                              │
                              ↓
                         [Show result]
```

That crossing tells you:

> **Responsibility moved from one actor to another.**

* * *

# 🔥 The real power of swimlanes

Swimlanes aren't just pretty flowcharts.

They're useful for finding **problems in a process**.

For example:

```

CUSTOMER
   │
   └── Submit application
            ↓
ADMIN
   │
   └── Review application
            ↓
MANAGER
   │
   └── Approve
            ↓
ADMIN
   │
   └── Process application
```

You can immediately ask:

> Why does it go from Admin → Manager → Admin?

Maybe that handoff is unnecessary.

Or:

> Why does the Manager take 3 days?

That's a **bottleneck**.

So swimlanes help you **understand → analyze → improve** a workflow.

* * *

# 🤖 For AI agents

This is especially useful for what you've been working on.

If you're giving an AI agent a workflow and want it to understand **who does what**, swimlanes are excellent.

A strong structure is:

```

Actor/Lane
   ↓
Action
   ↓
Decision
   ↓
Action
   ↓
Handoff to another Actor
   ↓
Next Action
```

For example:

```

USER
  ↓
[Submit Video URL]
  ↓
WEB APP
  ↓
[Validate URL]
  ↓
<Valid?>
  ├── NO → [Show Error] → USER
  │
  └── YES
       ↓
DOWNLOAD SERVICE
       ↓
[Fetch Video]
       ↓
<Download Successful?>
  ├── NO → [Return Error] → WEB APP
  │
  └── YES
       ↓
[Save Video]
       ↓
USER
       ↓
[View Downloaded Video]
```

An AI can understand this very quickly because **the actor, action, decision, and handoff are explicit**.

* * *

## 🧠 One-sentence definition

> **A swimlane diagram is a flowchart divided into lanes, where each lane represents a person, team, system, or organization responsible for part of the process.**

### Remember it like this:

**Flowchart = What happens?**

**Swimlane = What happens + Who does it?**

**Swimlane + decisions = What happens + Who does it + What happens depending on the result?**

* * *

## Bonus 🔥 Cheat code

When designing a swimlane workflow, think in **4 questions**:

> **WHO → DOES WHAT → DECIDES WHAT → HANDS OFF TO WHO?**

If your diagram answers those four questions, you've probably captured the workflow correctly.

And for AI-generated swimlanes, explicitly defining **lanes + symbols + arrows + decision branches + start/end** makes the output much more consistent than simply saying _"make a swimlane diagram."_

### Prompt
/NO-BS
Means lane can can have thier own workflow or flowchart

### Reply
Yes. **Exactly.**

A **lane can contain its own sequence of workflow steps**, just like a mini flowchart.

Example:

```

CUSTOMER
  [Start]
     ↓
  [Submit Order]
     ↓
  [Receive Confirmation]

WEBSITE
  [Receive Order]
     ↓
  [Validate Order]
     ↓
  <Valid?>
   ├─ No → [Show Error]
   └─ Yes → [Send to Payment]

PAYMENT
  [Process Payment]
     ↓
  <Successful?>
   ├─ No → [Payment Failed]
   └─ Yes → [Payment Approved]
```

The important distinction:

- **Lane** = **who/what owns the work**

- **Steps inside the lane** = that actor's **workflow**

- **Arrows between lanes** = **handoffs/interactions**

- **Decision** = branch in that workflow

So yes, you can think of a swimlane as **multiple mini-flowcharts running alongside each other and communicating with one another**.

### 🔥 Key point

A lane doesn't necessarily have to contain just **one action**.

It can contain:

> **Start → Process → Decision → Process → End**

while another lane does its own:

> **Receive → Process → Decision → Send**

The swimlane combines all those workflows into **one cross-functional process**.

### Prompt
/example
Copy past prompt to ask Claud ai to turn my .md file into a Swimlane flowchart
Make it a reusable prompts so Claude should understand what’s in file perfectly so it don’t assume

### Reply
Absolutely. The key is to tell Claude **the `.md` file is the source of truth** and that it must **not invent, assume, reorder, or add workflow steps**.

Here’s a reusable prompt you can copy-paste:

```

# TASK: Convert Markdown Workflow into a Swimlane Flowchart

I have attached a `.md` file containing a process/workflow.

Your job is to convert the contents of the Markdown file into a clear **swimlane flowchart**.

## SOURCE OF TRUTH

The attached `.md` file is the ONLY source of truth.

Before creating the diagram:

1. Read the entire `.md` file.
2. Understand the complete workflow and all relationships described in it.
3. Identify every actor, person, team, department, system, service, or external party involved.
4. Identify every action/process, decision, input, output, document, and handoff.
5. Preserve the workflow's original meaning and sequence.

## STRICT NO-ASSUMPTION RULE

DO NOT:
- Invent missing steps.
- Add actions that are not stated or clearly implied by the file.
- Remove important steps.
- Change the order of operations.
- Invent actors or departments.
- Assume responsibilities that the file does not assign.
- Assume a decision outcome that is not specified.
- Create fictional error-handling paths.
- Fill gaps with "common sense."
- Rewrite the business logic.
- Add features or processes because they seem logical.

If something is unclear, ambiguous, missing, or contradictory in the `.md` file:

→ Preserve the ambiguity rather than guessing.

If necessary, mark it as:
`[Unclear from source]`

## SWIMLANE RULE

Each lane represents an actor responsible for work.

An actor can be:
- A person
- A team
- A department
- A system
- A software service
- A customer
- An external organization/vendor

IMPORTANT:

A lane CAN contain its own complete workflow.

For example:

Actor A:
[Start] → [Process] → [Decision] → [Process]

Actor B:
[Receive] → [Process] → [Decision] → [Send]

These individual workflows should remain inside their respective lanes.

Connect the lanes when responsibility, information, data, documents, or materials move from one actor to another.

## SYMBOLS

Use standard flowchart logic.

Use these symbols only:

1. [Start/End]
   - Beginning or termination of the process.

2. [Process]
   - One specific action or operation.

3. [Decision]
   - A question that creates a branch.
   - Clearly show the branch labels such as YES / NO.

4. [Document]
   - A document or generated/received document.

5. [Data/Input]
   - Data entering or leaving a process.

6. [Subprocess]
   - A referenced workflow or process that contains multiple steps.

7. [Connector]
   - Used only when necessary to keep the diagram readable.

## FLOW RULES

- Every process step must belong to the lane of the actor responsible for performing it.
- Keep sequential steps in their actual order.
- Arrows must show the actual direction of the workflow.
- Arrows may remain inside a lane.
- Arrows may cross between lanes.
- Crossing into another lane represents a handoff or interaction.
- Decisions must have explicit branches.
- Do not create branches unless the source describes them.
- Do not merge separate workflows unless the source indicates they connect.
- Do not create a fake linear flow when the source describes parallel activity.
- Preserve loops, retries, approvals, escalations, and returns when they exist in the source.

## LANE IDENTIFICATION

Determine lanes ONLY from the `.md` file.

If the file says:

"Customer submits the request."

Create:

Customer
  → [Submit Request]

If the file says:

"System validates the request."

Create:

System
  → [Validate Request]

If the file says:

"Admin reviews and approves the request."

Create:

Admin
  → [Review Request]
  → [Approve Request]

Do not combine these into one generic "Process" lane.

## DECISION RULE

When the Markdown describes a condition, represent it as a decision.

Example:

"After validation, if the request is valid, continue. Otherwise show an error."

Represent it as:

[Validate Request]
       ↓
<Is Request Valid?>
   ├── YES → [Continue]
   └── NO  → [Show Error]

Do not invent additional outcomes.

## PARALLEL WORK

If the Markdown describes two or more activities happening independently or simultaneously:

- Keep them in their appropriate lanes.
- Do not force them into a single sequential chain.
- Show the relationship between them only if the source describes one.

## OUTPUT REQUIREMENTS

First provide a short:

### Workflow Interpretation

List:

- Lanes identified
- Main workflow
- Decisions identified
- Major handoffs
- Any ambiguities or missing information

Then provide:

### Swimlane Flowchart

Generate the actual swimlane diagram.

Use a format that clearly represents:

LANE
  ↓
[Process]
  ↓
[Decision]
  ├── YES → [Process]
  └── NO → [Process]
  ↓
OTHER LANE
  ↓
[Process]

## VALIDATION

Before finalizing, compare the generated diagram against the `.md` file.

Verify:

- Every important workflow step is represented.
- Every actor is represented.
- Every step is assigned to the correct actor.
- The sequence is preserved.
- Decisions are preserved.
- Decision branches are preserved.
- Handoffs are preserved.
- Loops are preserved.
- Parallel workflows are preserved.
- Nothing was invented.
- Nothing important was omitted.

If the source does not provide enough information to determine something, DO NOT GUESS.

Clearly mark it as:

[Unclear from source]

The goal is **faithful conversion**, not workflow redesign.

The `.md` file is the source of truth.
```

### 🔥 Even stronger version

If you want Claude to be **extremely strict**, add this at the very top:

```

IMPORTANT:

Do not use your general knowledge to "improve" the workflow.

Your task is NOT to design a workflow.

Your task is to TRANSCRIBE/TRANSLATE the workflow described in the Markdown file into a swimlane representation.

Think of the Markdown file as a legal specification:
- If it says it → include it.
- If it does not say it → do not invent it.
- If it is ambiguous → preserve the ambiguity.
- If two statements conflict → report the conflict.
- If information is missing → mark it missing.

Accuracy to the source is more important than making the diagram look complete.
```

**That's the important cheat code:** you're telling Claude **"convert, don't design."** This dramatically reduces the tendency to fill gaps with assumptions.

### Prompt
/summary

This is a \*\*swimlane / cross-functional flowchart\*\* — specifically a technical process diagram, not a UML diagram or a business process (BPMN) diagram, though it borrows conventions from both.

\*\*What it's good for / who can use it:\*\*
\- Developers or technical readers who want to see \*who does what and when\* across a system — here, the boundary between your code, the external server (Amazon), and the human running the script
\- Anyone auditing or debugging the scraper's logic flow without reading all the code
\- Documentation for a handoff (e.g., if you gave this script to a collaborator, this shows the shape of the pipeline at a glance)
\- A reference for extending the workflow later (e.g., "where would I add retry logic on failed requests?" — you can see exactly where that decision would slot in)

\*\*Who it's \*not\* for / limitations:\*\*
\- Non-technical stakeholders — it still assumes some familiarity with concepts like sessions, parsing, and async loops; it's not simplified enough for a "business audience" swimlane
\- Anyone needing exact timing/performance info — it shows sequence and logic, not latency, retries, or concurrency limits
\- A replacement for the code itself — it's faithful to the guide's \*described\* logic, not a guarantee the diagram matches whatever code you actually run (e.g., if you change selectors or add error handling, the diagram goes stale until you regenerate it)
\- Formal BPMN/UML tooling contexts — this uses your own 7-symbol/5-rule convention, not a standardized notation, so it won't import into tools expecting strict BPMN or UML syntax

Given it's built for \*your\* scraper (not AETHERIS), want me to hold onto this file, or would you rather this format become the reusable template for future non-AETHERIS technical workflows too?

### Reply
### Short summary

This is a **swimlane / cross-functional technical flowchart**.

**Purpose:**

Shows **who does what, when, and how work/data moves between them**—for example:

> Human → Your Code → Amazon Server → Your Code → Human

**Good for:**

- Understanding technical workflows quickly

- Debugging and auditing logic

- Documentation and handoffs

- Planning where to add features like retries or error handling

**Limitations:**

- Mainly useful for technical readers

- Shows logic, not performance/timing

- Doesn't replace the actual code

- Not strict BPMN or UML notation

### Core idea 🔥

It is essentially:

> **Multiple actors + their own workflows + connections/handoffs between those workflows = Swimlane diagram.**

And for your reusable system, the important rule is:

> **The source file is the source of truth. The diagram should represent the workflow, not redesign or assume it.**

### Prompt
What if I need something like how n8n or more closely and easy for the main code to reference and understand diagram is but in swimlane , what type of approach would feat

### Reply
If you want something **closer to n8n** but still in **swimlane format**, the best approach is a:

## **Swimlane Workflow / Executable Process Map**

Not a traditional business swimlane.

Think:

> **n8n workflow logic + swimlane ownership**

### Example

```

USER                 APPLICATION              API SERVER
 │                       │                       │
 │ [Submit URL] ───────→ │                       │
 │                       │ [Validate URL]        │
 │                       │        ↓              │
 │                       │   <Valid?>            │
 │                       │    ├─ NO ─────────→ [Return Error]
 │                       │    │
 │                       │    └─ YES
 │                       │         ↓
 │                       │ [HTTP Request] ─────→ │
 │                       │                       │ [Fetch Data]
 │                       │                       │      ↓
 │                       │ ←────── [Response] ───│
 │                       │
 │ ←──── [Display Data]  │
```

The key difference is that **each box behaves like an n8n node**, while the **lane tells you who owns/executes that node**.

* * *

# 🧠 Best mental model

Instead of:

> "Draw a flowchart showing who does what."

Tell the AI:

> **"Build a node-based workflow diagram where every operation is a node, every lane represents the node's executor/owner, and connections represent data/control flow."**

That's much closer to n8n.

### Your three layers become:

**Lane = WHO**

**Node = WHAT**

**Connection = HOW IT MOVES**

So:

```

LANE
 ├── [Node]
 ├── [Node]
 ├── <Decision Node>
 └── [Node]
          │
          └────────→ Another Lane
```

* * *

# ⚡ Make it even more useful for code

If the **main code needs to reference the diagram**, I would go one step further.

Use **unique node IDs**.

```

USER
[U01] Submit URL
      │
      ↓
APP
[A01] Validate URL
      │
      ↓
[A02] Is URL valid?
   ├── YES → [A03] Create Request
   └── NO  → [A04] Return Error
                       │
                       ↓
USER
                  [U02] Display Error
```

Now your code/documentation can refer to:

```

A01 = URL validation
A02 = validation decision
A03 = request creation
A04 = error response
```

That is **much easier for an AI agent or developer to reference** than vague descriptions like "the validation step."

* * *

# 🔥 The approach I'd recommend for you

Call the format:

### **Node-Based Swimlane Workflow**

or, even better:

### **Executable Swimlane Workflow**

Structure it around:

```

┌─────────────── LANE ───────────────┐
│                                    │
│ [NODE-ID] Action                   │
│       ↓                            │
│ [NODE-ID] Action                   │
│       ↓                            │
│ [NODE-ID] Decision                 │
│     ↙        ↘                     │
│   YES          NO                   │
│    ↓            ↓                  │
└────┼────────────┼──────────────────┘
     │            │
     ↓            ↓
┌─────────────── LANE ───────────────┐
│ [NODE-ID] Action                   │
└────────────────────────────────────┘
```

### Each node should have:

```

ID
Actor/Lane
Type
Action
Input
Output
Next node
Conditions
```

For example:

```

ID: A02
Lane: Application
Type: Decision
Action: Is URL valid?
Input: Submitted URL
Output: valid / invalid

YES → A03
NO  → A04
```

Now you've got something that is **visual like n8n**, **organized like a swimlane**, and **structured enough for code/AI to reason about**.

* * *

## 🧠 The big advantage

A normal swimlane says:

> "The application validates the URL."

A node-based swimlane says:

> **A01 → Application → Process → Validate URL → input: URL → output: valid/invalid → next: A02**

That's far more useful when you're building software.

**If your goal is for Claude/AI agents to read the diagram and reliably understand the actual code workflow, I'd choose this approach over a traditional swimlane.**

### Prompt
When is timeline used and why

### Reply
A **timeline** is used when **time is the main thing you want to understand**.

Think of it as:

> **What happens → and WHEN does it happen?**

### Example

For a software project:

```

MON          TUE          WED          THU
 │            │            │            │
[Design] → [Code] → [Testing] → [Deploy]
```

It helps you see **sequence across time, dates, duration, deadlines, and milestones**.

### When to use a timeline

- 📅 **Project planning** — what happens this week/month?

- 🚀 **Product development** — design → development → testing → launch

- 🏗️ **Construction** — foundation → building → inspection → completion

- 🎬 **Events** — preparation → event → cleanup

- 📜 **History** — event A → event B → event C

- 🔄 **Incident investigation** — error occurred → investigation → fix → recovery

- 👥 **Project coordination** — when different teams need to do their work

### Timeline vs Swimlane

This is the important distinction:

| Diagram | Main question |
| --- | --- |
| **Flowchart** | What happens next? |
| **Swimlane** | Who does what? |
| **Timeline** | When does it happen? |
| **Gantt chart** | When does each task start/end and how long does it take? |
| **n8n-style workflow** | What node connects to what? |

### 🔥 They can be combined

For complex technical workflows, you can have:

**Swimlane + Timeline**

```

TIME →     10:00       10:05       10:10

USER       [Submit]
              │
APP                  [Validate]
                         │
API                              [Process]
                                    │
USER                                         [Result]
```

Now you know **WHO**, **WHAT**, **WHEN**, and **the handoff between them**.

**Cheat code:** If removing the time information would make the diagram lose important meaning, use a timeline. If time doesn't matter and you're mainly tracking responsibility/logic, use a swimlane.

### Prompt
Uploaded an image

So what name can I call somthing like this

### Reply
The best name for something like this is:

## **Technical Swimlane Sequence Diagram**

More specifically, I’d call it:

> **Cross-Functional Technical Execution Flow**

Because this diagram combines several things:

- **Swimlanes** → User Interaction, Client Side, Server Side, Database

- **Execution sequence** → top-to-bottom order matters

- **System interactions** → client → server → database → client

- **Technical components** → Client Scripts, UI Policies, Business Rules, Workflows

- **Data operations** → queries and database updates

- **Event-driven behavior** → OnLoad, OnChange, OnSubmit, etc.

### It's NOT really a timeline

The vertical position mainly represents **execution order**, not actual clock time.

So:

```

OnLoad
  ↓
OnChange
  ↓
OnSubmit
  ↓
Business Rule
  ↓
Database Update
```

means **this happens in this order**, not necessarily "10:01 → 10:02 → 10:03."

### It's also more than a normal swimlane

A normal swimlane might be:

```

USER       → Submit
CLIENT     → Validate
SERVER     → Process
DATABASE   → Update
```

Your example goes deeper:

```

USER
  ↓
CLIENT
  ↓
  OnLoad Client Script
  ↓
  OnLoad UI Policy
  ↓
  OnChange Client Script
  ↓
SERVER
  ↓
  Business Rule
  ↓
DATABASE
  ↓
  Database Update
  ↓
SERVER
  ↓
CLIENT
```

So I'd use **Technical Execution Swimlane** as the short name.

### 🔥 If you're creating your own diagram standard

I'd recommend this name:

> **Execution-Ordered Swimlane Diagram**

**Definition:**

_A swimlane diagram where each lane represents an actor, system, or execution layer, while the flow direction explicitly represents the order in which operations, events, and handoffs occur._

That's probably the closest description to the image **and** to the n8n-like technical workflow you're trying to create.

### Prompt
Give me a reusable prompt I can use
And I want you to tell me when to split the prompt or not so ai agent can adapt properly and everything work well like prod

### Reply
Yes. If your goal is **production-quality diagrams that an AI agent can reliably understand and that stay faithful to your `.md`/code**, I would **not** use one giant "make me a diagram" prompt for complex workflows.

The strongest approach is:

> **Source → Extract workflow model → Validate → Render diagram**

That gives the AI an intermediate representation instead of forcing it to understand the source and draw at the same time.

# 1\. Reusable master prompt

Use this when the workflow is small/medium and you want everything in one pass:

```

# ROLE

You are a technical workflow architect and diagram generator.

Your task is to convert the attached source material into a:

TECHNICAL EXECUTION SWIMLANE DIAGRAM

This is NOT a generic business-process diagram.

The diagram must behave like a combination of:

- n8n-style node workflow
- technical execution flow
- cross-functional swimlane
- sequence/execution-order diagram

The goal is to make the workflow understandable to both:
1. humans/developers
2. AI agents that need to reason about the workflow

---

# SOURCE OF TRUTH

The attached file is the authoritative source.

Read and understand the ENTIRE source before generating the diagram.

Do not rely on assumptions, general knowledge, typical architecture, or what you think the system "should" do.

Represent what the source actually says.

If something is not specified:

DO NOT INVENT IT.

If something is ambiguous:

MARK IT AS:

[UNCLEAR FROM SOURCE]

If two parts of the source conflict:

MARK THE CONFLICT instead of silently choosing one.

---

# CORE MODEL

The diagram consists of:

LANES
+
NODES
+
CONNECTIONS
+
EXECUTION ORDER

## LANES

Each lane represents an actual execution owner or environment.

Examples:

- User
- Browser
- Client
- Frontend
- Backend
- API
- Worker
- External Service
- Database
- Queue
- Third-party system

Create lanes ONLY when supported by the source.

---

# NODES

Every meaningful operation should become a node.

Each node should represent ONE meaningful action/event.

Examples:

[Receive Request]

[Validate Input]

[HTTP Request]

[Query Database]

[Parse Response]

[Save Record]

[Return Response]

[OnSubmit]

[Business Rule]

[Async Job]

Do not combine unrelated operations into one node.

---

# NODE IDS

Give every meaningful node a unique stable ID.

Example:

U01
C01
C02
S01
S02
DB01

The ID must remain stable throughout the diagram.

Example:

C01 = Validate URL
C02 = Create Request
S01 = Fetch Data
DB01 = Query Database

Use the IDs when explaining relationships.

---

# NODE TYPES

Use these concepts:

START
END
PROCESS
DECISION
INPUT
OUTPUT
DOCUMENT
DATABASE
EXTERNAL SERVICE
SUBPROCESS
ASYNC
EVENT

Use the simplest appropriate type.

---

# EXECUTION ORDER

The primary flow direction represents EXECUTION ORDER.

Normally:

TOP → BOTTOM

or

LEFT → RIGHT

depending on the layout.

Do NOT interpret position as elapsed time unless the source explicitly provides timing.

The diagram must make clear:

WHAT HAPPENS FIRST
↓
WHAT HAPPENS NEXT
↓
WHAT HAPPENS AFTER THAT

---

# SWIMLANE RULE

A lane may contain its OWN complete workflow.

For example:

CLIENT
  [C01] OnLoad
      ↓
  [C02] Validate
      ↓
  [C03] OnChange
      ↓
  [C04] Submit

SERVER
  [S01] Receive Request
      ↓
  [S02] Validate
      ↓
  [S03] Process

DATABASE
  [DB01] Query
      ↓
  [DB02] Update

Do not force every lane into a single straight line.

---

# CROSS-LANE HANDOFFS

When execution moves from one actor/system to another, show a connection across lanes.

Example:

CLIENT
[C04] Submit
      │
      └──────────────→ SERVER
                       [S01] Receive Request

SERVER
[S03] Query Database
      │
      └──────────────→ DATABASE
                       [DB01] Query

DATABASE
[DB01] Result
      │
      └──────────────→ SERVER
                       [S04] Process Result

SERVER
[S05] Return Response
      │
      └──────────────→ CLIENT
                       [C05] Display Result

---

# DECISIONS

Every actual conditional branch must be represented.

Example:

[S02] Validate Request
        ↓
   <Valid?>
    /     \
  YES     NO
   ↓       ↓
[S03]    [S04]
Continue  Return Error

Never invent branches.

If the source only specifies YES:

<Valid?>
  └─ YES → Continue

Do not automatically invent:

NO → Error

---

# LOOPS

Preserve loops exactly when they exist.

Example:

[Process]
   ↓
<Successful?>
 ├─ YES → [Continue]
 └─ NO  → [Retry]
             │
             └────→ [Process]

Do not create retries unless the source specifies them.

---

# ASYNC / PARALLEL EXECUTION

Do not incorrectly turn asynchronous or parallel operations into sequential operations.

If the source says something happens asynchronously:

represent it as:

[ASYNC] Operation

If multiple operations execute independently:

show separate branches.

Only join them when the source indicates that they converge.

---

# DATA FLOW

Where useful, label connections with what is being transferred.

Examples:

-- request -->

-- response -->

-- user_id -->

-- database result -->

-- event -->

-- document -->

This is especially important for technical workflows.

---

# ERROR PATHS

Represent error handling ONLY when present in the source.

Examples:

[Request]
   ↓
<Success?>
 ├─ YES → [Process]
 └─ NO  → [Error Response]

Do not manufacture error handling.

---

# SUBPROCESSES

If the source references a large internal process:

Use:

[SUBPROCESS: Process Name]

instead of unnecessarily expanding everything into the parent workflow.

However, if the source contains the details of that subprocess and the requested output requires them, expand it into its own workflow/lane or section.

---

# EXTERNAL SYSTEMS

Clearly distinguish:

INTERNAL
EXTERNAL
DATABASE
USER

For example:

CLIENT → API → AMAZON → API → CLIENT

Do not treat an external service as if it were part of the application's internal code.

---

# IMPORTANT DISTINCTION

This diagram is NOT a timeline.

Vertical/horizontal position represents:

EXECUTION ORDER

not:

CLOCK TIME

Only show actual timing when the source explicitly states timing.

---

# OUTPUT

Return these sections in this exact order:

## 1. Workflow Summary

Briefly explain what the workflow does.

## 2. Lanes

List every lane and what it represents.

Example:

- User — initiates interaction
- Client — executes browser logic
- Server — processes request
- Database — stores/retrieves data

## 3. Execution Map

List the nodes in execution order using their IDs.

Example:

U01 → C01 → C02 → S01 → DB01 → S02 → C03

Include branches where necessary.

## 4. Swimlane Diagram

Generate the actual technical swimlane diagram.

## 5. Validation Report

Check:

[ ] Every important source step is represented
[ ] Every actor is represented
[ ] Every node has a unique ID
[ ] Node ownership is correct
[ ] Execution order is preserved
[ ] Decisions are preserved
[ ] Branches are preserved
[ ] Loops are preserved
[ ] Async operations are preserved
[ ] Cross-lane handoffs are preserved
[ ] No unsupported steps were invented
[ ] No important source information was omitted

## 6. Uncertainties

List anything that could not be determined from the source.

---

# FINAL RULE

SOURCE FIDELITY > VISUAL COMPLETENESS.

It is better to produce:

[UNCLEAR FROM SOURCE]

than to guess.

Do not redesign the workflow.

Do not optimize the workflow.

Do not "fix" the workflow.

Convert the source faithfully into a technical execution swimlane.
```

* * *

# 2\. But for serious/large workflows: SPLIT IT 🔥

This is what I'd recommend for **production use**.

Don't ask the AI to:

> read 100 pages → understand everything → decide lanes → decide nodes → resolve dependencies → draw diagram

all in one shot.

Instead use **3 stages**.

## Stage 1 — Extract

Give Claude:

```

Read the attached source completely.

Do NOT create a diagram yet.

Extract the workflow into a structured execution model.

Identify:

1. Actors/systems
2. Lanes
3. Events
4. Actions
5. Decisions
6. Inputs
7. Outputs
8. Database operations
9. External services
10. Handoffs
11. Loops
12. Async operations
13. Parallel operations
14. Error paths
15. Subprocesses
16. Execution dependencies

For every node create:

ID:
Lane:
Type:
Action:
Input:
Output:
Trigger:
Next:
Conditions:
Async:
Source reference:

IMPORTANT:

The source is the only source of truth.

Do not invent missing information.

Mark uncertainty as:

[UNCLEAR FROM SOURCE]

Do not generate the diagram yet.

At the end, provide the complete execution model.
```

### Why this is powerful

Now Claude creates something like:

```

C01
Lane: Client
Type: Process
Action: Validate URL
Input: User URL
Output: Validated URL
Next: C02
Source: section 2.3

C02
Lane: Client
Type: Decision
Action: Is URL valid?
Next:
YES → C03
NO → C04
```

That's much easier to validate.

* * *

# 3\. Stage 2 — Validate the model

Then give Claude:

```

Review the extracted workflow model against the original source.

Do NOT create the diagram yet.

Perform a strict consistency audit.

Check:

1. Missing nodes
2. Invented nodes
3. Incorrect lane assignments
4. Incorrect execution order
5. Missing branches
6. Incorrect branches
7. Missing loops
8. Missing async behavior
9. Missing parallel behavior
10. Incorrect database interactions
11. Incorrect external-system interactions
12. Broken dependencies
13. Duplicate nodes
14. Ambiguous relationships

For every problem:

IDENTIFY:
- Node ID
- Problem
- Evidence from source
- Required correction

Do not guess.

After validation, output a corrected canonical workflow model.

This canonical model becomes the source of truth for diagram generation.
```

This is your **quality-control stage**.

* * *

# 4\. Stage 3 — Render the diagram

Then:

```

Using ONLY the validated canonical workflow model above:

Generate a TECHNICAL EXECUTION SWIMLANE DIAGRAM.

Do not reinterpret the workflow.

Do not add new nodes.

Do not remove nodes.

Do not change execution order.

Do not invent relationships.

Do not optimize the workflow.

Requirements:

- Each lane represents an execution owner.
- Each meaningful operation is a node.
- Every node has its stable ID.
- Connections represent execution/data/control flow.
- Cross-lane connections represent handoffs.
- Decisions show explicit branches.
- Loops remain loops.
- Async operations remain async.
- Parallel operations remain parallel.
- Database operations remain associated with the database lane.
- External systems remain external.
- Execution order must be obvious.

Generate the diagram using [YOUR PREFERRED FORMAT].

Then perform a final diagram-vs-model validation and report any discrepancy.
```

* * *

# 🧠 When should you split it?

Use this rule:

| Workflow | Approach |
| --- | --- |
| Small, <20 nodes | **1 prompt** |
| Medium, 20–50 nodes | **1 prompt + validation** |
| Large, 50–100 nodes | **3-stage approach** |
| Huge/complex system | **3-stage + split into subprocesses** |
| Codebase → diagram | **Extract → Validate → Render** |
| Diagram must be used by AI agents | **Always create structured node model** |
| Mission-critical documentation | **Always split** |

* * *

# 🔥 The real "production" architecture

If you're building this into an AI tool, I'd actually make the pipeline:

```

              SOURCE
                │
                ▼
        ┌─────────────────┐
        │  SOURCE PARSER  │
        └────────┬────────┘
                 ↓
        ┌─────────────────┐
        │ WORKFLOW MODEL  │
        │     JSON        │
        └────────┬────────┘
                 ↓
        ┌─────────────────┐
        │    VALIDATOR    │
        └────────┬────────┘
                 ↓
        ┌─────────────────┐
        │ CANONICAL MODEL │
        └────────┬────────┘
                 ↓
        ┌─────────────────┐
        │ DIAGRAM RENDERER│
        └────────┬────────┘
                 ↓
        ┌─────────────────┐
        │ SWIMLANE DIAGRAM│
        └─────────────────┘
```

The **JSON workflow model** is the secret weapon.

For example:

```
JSON

{
  "lanes": [\
    {\
      "id": "client",\
      "name": "Client"\
    },\
    {\
      "id": "server",\
      "name": "Server"\
    },\
    {\
      "id": "database",\
      "name": "Database"\
    }\
  ],
  "nodes": [\
    {\
      "id": "C01",\
      "lane": "client",\
      "type": "process",\
      "label": "Validate URL",\
      "next": ["C02"]\
    },\
    {\
      "id": "C02",\
      "lane": "client",\
      "type": "decision",\
      "label": "URL valid?",\
      "branches": {\
        "yes": "S01",\
        "no": "C03"\
      }\
    }\
  ]
}
```

Now you have **one source of truth**.

You can render that same model as:

- 🏊 Swimlane

- 🔀 n8n-style workflow

- 📊 Flowchart

- 🧩 Dependency graph

- 📜 Execution documentation

- 🤖 AI-readable workflow

- 💻 Potentially even executable automation

### The biggest cheat code 🔥

**Don't make the diagram your source of truth.**

Make the **structured workflow model** your source of truth.

Then:

> **Markdown/code → Workflow Model → Swimlane**

rather than:

> Markdown/code → Pretty Diagram

That distinction is what makes the system much more reliable when you eventually want an AI agent to **reference, modify, debug, or regenerate** the workflow.

### Prompt
Means all this prompt seats like variables ai should inject ?

### Reply
Yes — **if you're building a reusable prompt/template**, you should think of the changing parts as **variables/placeholders** that your AI system injects.

But **don't make the whole prompt variables**. Keep the rules fixed and inject only the things that change.

### Think of it like this

```

FIXED SYSTEM RULES
        +
VARIABLE INPUTS
        ↓
   AI AGENT
        ↓
WORKFLOW MODEL
        ↓
SWIMLANE DIAGRAM
```

### Fixed

These should stay in your master prompt:

```

- Source is the source of truth
- Don't assume
- Don't invent steps
- Lanes represent execution owners
- Nodes represent actions
- IDs must be unique
- Preserve execution order
- Preserve decisions
- Preserve loops
- Preserve async operations
- Validate before rendering
```

### Variables

These are injected depending on the job:

```

{{SOURCE_FILE}}

{{DIAGRAM_TYPE}}

{{OUTPUT_FORMAT}}

{{SYMBOL_RULES}}

{{LANE_RULES}}

{{NODE_RULES}}

{{DOMAIN}}

{{AUDIENCE}}

{{MAX_COMPLEXITY}}

{{RENDER_DIRECTION}}
```

For example:

```

SOURCE_FILE = scraper.md

DIAGRAM_TYPE = Technical Execution Swimlane

OUTPUT_FORMAT = Mermaid

DOMAIN = Web scraping

AUDIENCE = Developers + AI agents

RENDER_DIRECTION = Top to bottom
```

Then the AI effectively receives:

```

Convert {{SOURCE_FILE}}

into {{DIAGRAM_TYPE}}

using {{OUTPUT_FORMAT}}

for {{AUDIENCE}}.

Domain:
{{DOMAIN}}

...

[fixed workflow rules]
```

## 🔥 Even better: separate configuration from instructions

If you're building an actual agent, I'd use a config object like:

```
JSON

{
  "source": {
    "type": "markdown",
    "file": "{{SOURCE_FILE}}"
  },
  "diagram": {
    "type": "technical_execution_swimlane",
    "format": "mermaid",
    "direction": "top_to_bottom"
  },
  "rules": {
    "source_of_truth": true,
    "allow_assumptions": false,
    "preserve_order": true,
    "preserve_decisions": true,
    "preserve_loops": true,
    "preserve_async": true,
    "unique_node_ids": true
  },
  "audience": [\
    "developer",\
    "ai_agent"\
  ]
}
```

Your **prompt is the behavior**, while this configuration is the **variables/settings**.

### One important distinction

Don't let the AI freely inject things like:

```

{{LANE_RULES}}
{{SYMBOL_RULES}}
{{NODE_RULES}}
```

unless **your application controls those values**.

Otherwise the AI can accidentally change its own rules.

For a reliable production setup:

**Your application controls configuration → AI reads configuration → AI processes source → validator checks result → renderer creates diagram.**

That's much more robust than asking Claude to decide everything dynamically.

